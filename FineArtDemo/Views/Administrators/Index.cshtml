@*@{
    ViewData["Title"] = "Index";
    var admins = ViewData["AdminList"] as List<FineArtDemo.Areas.Identity.Data.CustomizeUser>;
    var manager = ViewData["ManagerList"] as List<FineArtDemo.Areas.Identity.Data.CustomizeUser>;
    var staff = ViewData["StaffList"] as List<FineArtDemo.Areas.Identity.Data.CustomizeUser>;
    var student = ViewData["StudentList"] as List<FineArtDemo.Areas.Identity.Data.CustomizeUser>;
}*@

<h2>Index</h2>

@{
    if (@TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Successed!</strong> @TempData["Success"]
        </div>
    }
}

<a asp-action="Create">Create Account</a>

@ViewData["test"]

@*<hr />
<h4>Admin Account List</h4>
<table class="table table-hover">
    <tr>
        <td>User Name</td>
        <td>Option</td>
    </tr>
    @{
        foreach (var item in admins)
        {
            <tr>
                <td>@item.UserName</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    }
</table>


<hr />
<h4>Manager Account List</h4>
<table class="table table-hover">
    <tr>
        <td>User Name</td>
        <td>Option</td>
    </tr>
    @{
        foreach (var item in manager)
        {
            <tr>
                <td>@item.UserName</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    }
</table>

<hr />
<h4>Staff Account List</h4>
<table class="table table-hover">
    <tr>
        <td>User Name</td>
        <td>Option</td>
    </tr>
    @{
        foreach (var item in staff)
        {
            <tr>
                <td>@item.UserName</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    }
</table>

<hr />
<h4>Student Account List</h4>
<table class="table table-hover">
    <tr>
        <td>User Name</td>
        <td>Option</td>
    </tr>
    @{
        foreach (var item in student)
        {
            <tr>
                <td>@item.UserName</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    }
</table>*@

